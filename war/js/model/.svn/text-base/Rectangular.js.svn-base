function Rectangular() {
}

Rectangular.prototype.getCenterX = function() {
    return this.shape.getAbsolutePosition().x + this.shape.getWidth() / 2
};

Rectangular.prototype.getCenterY = function() {
    return this.shape.getAbsolutePosition().y + this.shape.getHeight() / 2;
};

